import { CustimisedFilterPipe } from './custimised-filter.pipe';

describe('CustimisedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustimisedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
